┌─────────────────────────────────────────────────────────────────────────────────┐
│                           petri_net_platform                                    │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                 │
│  ┌─────────────────────────────────────────────────────────────────────────┐    │
│  │                          核心层                                          │    │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────────┐  │   │
│  │  │architecture │  │  marking    │  │  petri_net  │  │    variable     │  │   │
│  │  │ (抽象接口)   │  │ (标识类)     │  │ (网实现)    │   │   (变量定义)     │  │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────────┘  │   │
│  └─────────────────────────────────────────────────────────────────────────┘    │
│                                       │                                         │
│                                       ▼                                         │
│  ┌─────────────────────────────────────────────────────────────────────────┐    │
│  │                       表示层                                             │    │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐                       │   │
│  │  │   graph     │  │  features   │  │   models    │                       │   │
│  │  │ (图结构)     │  │ (特征编码)   │  │ (GCN模型)    │                       │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘                       │   │
│  └─────────────────────────────────────────────────────────────────────────┘    │
│                                       │                                         │
│                                       ▼                                         │
│  ┌─────────────────────────────────────────────────────────────────────────┐    │
│  │                        搜索层                                            │    │
│  │  ┌───────────────┐  ┌─────────────────┐  ┌─────────────────────────┐    │    │
│  │  │abstract_search│  │rl_env_semantics │  │  deadlock_controller    │    │    │
│  │  │ (搜索基类)    │  │ (环境语义)      │  │  (死锁控制器)               │    │    │
│  │  └───────────────┘  └─────────────────┘  └──────────────────────────────┘    │
│  │  ┌───────────────────────────────────────────────────────────────────┐  │    │
│  │  │              petri_net_gcn_ppo (PPO强化学习搜索)                    │  │    │
│  │  └───────────────────────────────────────────────────────────────────┘  │    │
│  └─────────────────────────────────────────────────────────────────────────┘    │
│                                       │                                         │
│                                       ▼                                         │
│  ┌─────────────────────────────────────────────────────────────────────────┐    │
│  │                        工具层                                            │    │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────────────────┐  │    │
│  │  │   result    │  │    link     │  │      其他工具类                  │  │    │
│  │  │ (结果封装)   │  │ (链表工具)   │  │                                  │  │   │
│  │  └─────────────┘  └─────────────┘  └─────────────────────────────────┘  │    │
│  └─────────────────────────────────────────────────────────────────────────┘    │
│                                                                                 │
└─────────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────────┐
│                              模块导入依赖关系                                     │
└─────────────────────────────────────────────────────────────────────────────────┘

petri_net_gcn_ppo.py
    │
    ├──► abstract_search.py (AbstractSearch 基类)
    │
    ├──► deadlock_controller.py (DeadlockController)
    │       │
    │       └──► rl_env_semantics.py (语义判断函数)
    │
    ├──► petri_gcn_models.py (PetriNetGCNEnhanced)
    │       │
    │       └──► representation/models.py (MultiScalePetriRepresentation)
    │               │
    │               ├──► representation/graph.py (PetriNetGraph)
    │               │
    │               └──► representation/features.py (PetriStateFeatureEncoder)
    │                       │
    │                       └──► deadlock_controller.py (绑定控制器)
    │
    ├──► rl_env_semantics.py (停止信息处理)
    │
    └──► utils/result.py (Result 结果封装)

petri_net.py
    │
    ├──► architecture.py (PetriNet, Marking 抽象类)
    │
    └──► marking.py (各种 Marking 实现)

┌─────────────────────────────────────────────────────────────────────────────────┐
│                              类继承层次结构                                       │
└─────────────────────────────────────────────────────────────────────────────────┘

【Marking 标识类层次】
                    
Marking (抽象基类)
    │
    ├──► AbstractMarking
    │       │
    │       └──► AbstractMarkingWithDeterminedPInfo
    │               │
    │               ├──► NormalMarking
    │               ├──► TTimeMarking
    │               ├──► TPPNMarking
    │               └──► TTPPNMarking
    │                       │
    │                       └──► TTPPNMarkingHasResidenceTime (+ HasResideceTime)
    │
    └──► HasResideceTime (混入类)
            │
            └──► TTPPNMarkingByTokenWithResTime

【PetriNet 网类层次】

PetriNet (抽象基类)
    │
    ├──► NormalPetriNet
    │       │
    │       └──► NormalPetriNetHasPostAndPre
    │
    ├──► TPPN (时间库所Petri网)
    │
    ├──► TTPPN (时间库所时间变迁Petri网)
    │       │
    │       └──► TTPPNHasPostAndPre
    │
    ├──► TTPPNHasResidenceTime (带驻留时间)
    │
    ├──► TTPPNByTokenWithResTime (按Token追踪驻留时间)
    │
    └──► TTimePetriNet (时间变迁Petri网)

【Search 搜索类层次】

Search (抽象基类)
    │
    └──► AbstractSearch
            │
            └──► PetriNetGCNPPOEnhanced
                    │
                    └──► PetriNetGCNPPOEnhancedHQ

┌─────────────────────────────────────────────────────────────────────────────────┐
│                              数据流向图                                           │
└─────────────────────────────────────────────────────────────────────────────────┘

┌─────────────┐      ┌─────────────┐      ┌─────────────────┐
│  初始配置   │      │  pre/post   │      │  min_delay_p/t  │
│  p_info     │      │  邻接矩阵   │      │  时间约束       │
└──────┬──────┘      └──────┬──────┘      └────────┬────────┘
       │                    │                      │
       └────────────────────┼──────────────────────┘
                            │
                            ▼
              ┌─────────────────────────────┐
              │      PetriNet 实例          │
              │  (TTPPNHasResidenceTime等)  │
              └─────────────┬───────────────┘
                            │
         ┌──────────────────┼──────────────────┐
         │                  │                  │
         ▼                  ▼                  ▼
┌─────────────┐    ┌─────────────┐    ┌─────────────────┐
│ get_marking │    │   enable    │    │     launch      │
│  获取标识   │    │ 检查使能    │    │    执行变迁     │
└──────┬──────┘    └──────┬──────┘    └────────┬────────┘
       │                  │                    │
       │                  │                    │
       │                  ▼                    │
       │          ┌─────────────┐              │
       │          │ is_enable[] │              │
       │          │ 使能状态表  │              │
       │          └──────┬──────┘              │
       │                 │                     │
       └─────────────────┼─────────────────────┘
                         │
                         ▼
              ┌─────────────────────────────┐
              │        Marking 标识         │
              │  - p_info[] (托肯分布)      │
              │  - t_info[] (时间信息)      │
              │  - prefix (全局时间)        │
              │  - residence_time_info      │
              │  - curr_delay_t[]           │
              │  - is_enable[]              │
              └─────────────┬───────────────┘
                            │
                            ▼
              ┌─────────────────────────────┐
              │   PetriNetGraph 图结构      │
              │  - pre/post 张量            │
              │  - 度特征预计算             │
              └─────────────┬───────────────┘
                            │
                            ▼
              ┌─────────────────────────────┐
              │ PetriStateFeatureEncoder    │
              │  特征编码                    │
              └─────────────┬───────────────┘
                            │
         ┌──────────────────┼──────────────────┐
         │                  │                  │
         ▼                  ▼                  ▼
┌─────────────┐    ┌─────────────┐    ┌─────────────────┐
│place_features│   │transition_  │    │ controller_     │
│ 库所特征    │    │  features   │    │   features      │
│ (7-8维)     │    │ 变迁特征    │    │ 控制器特征      │
│             │    │ (12维)      │    │                 │
└──────┬──────┘    └──────┬──────┘    └────────┬────────┘
       │                  │                    │
       └──────────────────┼────────────────────┘
                          │
                          ▼
              ┌─────────────────────────────┐
              │ PetriRepresentationInput    │
              │  GCN输入封装                 │
              └─────────────┬───────────────┘
                            │
                            ▼
              ┌─────────────────────────────┐
              │ MultiScalePetriRepresentation│
              │  GCN 主干网络                │
              └─────────────┬───────────────┘
                            │
         ┌──────────────────┼──────────────────┐
         │                  │                  │
         ▼                  ▼                  ▼
┌─────────────┐    ┌─────────────┐    ┌─────────────────┐
│place_       │    │transition_  │    │  graph_         │
│ embeddings  │    │ embeddings  │    │  embedding      │
│ 库所嵌入    │    │ 变迁嵌入    │    │  图嵌入         │
└─────────────┘    └──────┬──────┘    └────────┬────────┘
                          │                    │
                          ▼                    ▼
                   ┌─────────────┐      ┌─────────────────┐
                   │transition_  │      │   value_head    │
                   │   logits    │      │   价值估计      │
                   │ 策略输出    │      │                 │
                   └──────┬──────┘      └────────┬────────┘
                          │                      │
                          └──────────┬───────────┘
                                     │
                                     ▼
                          ┌─────────────────────────────┐
                          │     PPO 训练/推理          │
                          │  - 动作选择                 │
                          │  - 价值函数更新             │
                          │  - 策略优化                 │
                          └─────────────┬───────────────┘
                                        │
                                        ▼
                          ┌─────────────────────────────┐
                          │        Result 结果          │
                          │  - trans[] (变迁序列)       │
                          │  - markings[] (标识序列)    │
                          └─────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────────┐
│                           PPO 训练完整调用链                                      │
└─────────────────────────────────────────────────────────────────────────────────┘

PetriNetGCNPPOEnhanced.train_model()
    │
    ├──► [循环 train_iterations 次]
    │       │
    │       ├──► _episode_step_limit(iter_idx)  ─────► 返回当前迭代的步数限制
    │       │
    │       ├──► [循环 rollout_episodes_per_iter 次]
    │       │       │
    │       │       └──► _collect_training_episode(step_limit)
    │       │               │
    │       │               ├──► _set_to_initial()  ─────► petri_net = initial_petri_net.clone()
    │       │               │
    │       │               ├──► [循环 step_limit 步]
    │       │               │       │
    │       │               │       ├──► _is_goal(curr_marking)  ─────► 检查是否到达目标
    │       │               │       │
    │       │               │       ├──► encoder.encode(state)
    │       │               │       │       │
    │       │               │       │       └──► PetriStateFeatureEncoder.encode(marking)
    │       │               │       │               │
    │       │               │       │               ├──► encode_places(marking)
    │       │               │       │               │       └──► 提取库所特征 (token, residence, degree等)
    │       │               │       │               │
    │       │               │       │               └──► encode_transitions(marking)
    │       │               │       │                       │
    │       │               │       │                       ├──► _get_enabled_transitions(marking)
    │       │               │       │                       │
    │       │               │       │                       ├──► _get_current_transition_delay(marking)
    │       │               │       │                       │
    │       │               │       │                       └──► _get_controller_transition_features(marking)
    │       │               │       │                               │
    │       │               │       │                               └──► deadlock_controller.analyze_state()
    │       │               │       │
    │       │               │       ├──► model(encoded)  ─────► PetriNetGCNActorCritic.forward()
    │       │               │       │       │
    │       │               │       │       ├──► actor_net._split_inputs(x_p)
    │       │               │       │       │
    │       │               │       │       ├──► actor_net._adapt_place_features()
    │       │               │       │       │
    │       │               │       │       ├──► actor_net._adapt_transition_features()
    │       │               │       │       │
    │       │               │       │       ├──► actor_net.backbone(place_features, transition_features)
    │       │               │       │       │       │
    │       │               │       │       │       └──► MultiScalePetriRepresentation.forward()
    │       │               │       │       │               │
    │       │               │       │       │               ├──► place_input / transition_input (线性投影)
    │       │               │       │       │               │
    │       │               │       │       │               ├──► [循环 num_layers 层]
    │       │               │       │       │               │       │
    │       │               │       │       │               │       └──► MultiScaleFusionLayer.forward()
    │       │               │       │       │               │               │
    │       │               │       │       │               │               ├──► 库所→变迁消息传递
    │       │               │       │       │               │               │
    │       │               │       │       │               │               ├──► 变迁→库所消息传递
    │       │               │       │       │               │               │
    │       │               │       │       │               │               └──► LayerNorm + FFN
    │       │               │       │       │               │
    │       │               │       │       │               ├──► graph_readout()  ─────► 图级嵌入
    │       │               │       │       │               │
    │       │               │       │       │               └──► transition_head()  ─────► 变迁logits
    │       │               │       │       │
    │       │               │       │       └──► value_head(graph_embedding)  ─────► 状态价值
    │       │               │       │
    │       │               │       ├──► _mask_from_marking(state)
    │       │               │       │       │
    │       │               │       │       └──► deadlock_controller.analyze_state(petri_net, marking)
    │       │               │       │               │
    │       │               │       │               ├──► enabled_transitions_for_marking()
    │       │               │       │               │
    │       │               │       │               ├──► [第一层硬过滤]
    │       │               │       │               │       │
    │       │               │       │               │       └──► _hard_block_reason()
    │       │               │       │               │               │
    │       │               │       │               │               ├──► has_over_residence_time()
    │       │               │       │               │               │
    │       │               │       │               │               └──► enabled_transitions_for_marking()
    │       │               │       │               │
    │       │               │       │               └──► [第二层前瞻检查]
    │       │               │       │                       │
    │       │               │       │                       └──► _apply_lookahead()
    │       │               │       │                               │
    │       │               │       │                               └──► _has_survival_path() (递归)
    │       │               │       │
    │       │               │       ├──► _select_action(logits, mask, greedy=False)
    │       │               │       │       │
    │       │               │       │       └──► Categorical(logits).sample()
    │       │               │       │
    │       │               │       ├──► petri_net.enable(action)  ─────► 检查动作是否可执行
    │       │               │       │
    │       │               │       ├──► petri_net.launch(action)  ─────► 执行变迁，获取新标识
    │       │               │       │
    │       │               │       ├──► _calc_reward()  ─────► 计算奖励
    │       │               │       │
    │       │               │       └──► 收集
    │       │               │
    │       │               ├──► _compute_gae(rewards, values, dones, last_value)
    │       │               │       │
    │       │               │       └──► 计算 GAE 优势估计和回报
    │       │               │
    │       │               └──► 返回 episode 数据字典
    │       │
    │       └──► _update_from_episodes(episodes)
    │               │
    │               ├──► encoder.encode_batch(states)
    │               │
    │               ├──► [循环 ppo_update_epochs 次]
    │               │       │
    │               │       ├──► model(encoded)  ─────► 获取新 logits 和 values
    │               │       │
    │               │       ├──► 计算 PPO 损失
    │               │       │       │
    │               │       │       ├──► policy_loss = -min(surr1, surr2)
    │               │       │       │
    │               │       │       ├──► value_loss = MSE(values, returns)
    │               │       │       │
    │               │       │       └──► entropy = dist.entropy()
    │               │       │
    │               │       ├──► optimizer.zero_grad()
    │               │       │
    │               │       ├──► loss.backward()
    │               │       │
    │               │       ├──► clip_grad_norm_()
    │               │       │
    │               │       └──► optimizer.step()
    │               │
    │               └──► 返回损失指标
    │
    ├──► _estimate_policy_success_rate()
    │       │
    │       └──► _estimate_rollout_success_rate()
    │               │
    │               └──► _single_rollout(greedy=True)
    │
    └──► [额外训练循环，直到达到目标成功率]

┌─────────────────────────────────────────────────────────────────────────────────┐
│                           推理/搜索调用链                                         │
└─────────────────────────────────────────────────────────────────────────────────┘

PetriNetGCNPPOEnhanced.search()
    │
    ├──► [如果未训练] train_model()
    │
    ├──► _single_rollout(greedy=True)
    │       │
    │       ├──► _set_to_initial()
    │       │
    │       ├──► [循环 inference_max_steps_per_episode 步]
    │       │       │
    │       │       ├──► _is_goal(curr_marking)
    │       │       │
    │       │       ├──► model(encoder.encode(curr_marking))
    │       │       │
    │       │       ├──► _mask_from_marking(curr_marking)
    │       │       │
    │       │       ├──► _select_action(logits, mask, greedy=True)
    │       │       │       │
    │       │       │       └──► argmax(masked_logits)
    │       │       │
    │       │       ├──► petri_net.enable(action)
    │       │       │
    │       │       └──► petri_net.launch(action)
    │       │
    │       └──► 返回 Result(trans, markings)
    │
    ├──► [如果有最佳训练序列]
    │       │
    │       └──► _result_from_trans(best_train_trans)
    │
    ├──► _pick_best(candidates)
    │       │
    │       └──► 选择最优结果 (目标达成 > 目标距离 > 时间前缀)
    │
    └──► 返回 best Result

┌─────────────────────────────────────────────────────────────────────────────────┐
│                     DeadlockController ↔ FeatureEncoder 交互                     │
└─────────────────────────────────────────────────────────────────────────────────┘

PetriNetGCNPPOEnhanced.__init__()
    │
    ├──► 创建 DeadlockController
    │
    ├──► 创建 PetriStateEncoderEnhanced
    │
    └──► encoder.bind_deadlock_controller(
            lambda: self.petri_net,    ◄─── 延迟获取当前网状态
            self.deadlock_controller,
            enabled=True
        )

┌─────────────────────────────────────────────────────────────────────────────────┐
│                           特征编码时的调用链                                      │
└─────────────────────────────────────────────────────────────────────────────────┘

PetriStateFeatureEncoder.encode_transitions(marking)
    │
    └──► _get_controller_transition_features(marking)
            │
            ├──► [检查控制器是否启用]
            │
            ├──► deadlock_controller.analyze_state(petri_net, marking)
            │       │
            │       ├──► enabled_transitions_for_marking()
            │       │
            │       ├──► _simulate_action() ──► petri_net.launch()
            │       │
            │       ├──► _hard_block_reason()
            │       │       │
            │       │       └──► has_over_residence_time()
            │       │
            │       └──► _apply_lookahead()
            │               │
            │               └──► _has_survival_path() (递归搜索)
            │
            └──► 返回控制器特征:
                    - controller_allowed[] (安全动作掩码)
                    - hard_blocked[] (硬过滤动作)
                    - soft_risk[] (高风险动作)
                    - safe_ratio (安全比例)
                    - fbm_candidate (FBM候选标志)

┌─────────────────────────────────────────────────────────────────────────────────┐
│                           GCN 网络内部数据流                                      │
└─────────────────────────────────────────────────────────────────────────────────┘

输入: place_features [num_places, place_dim]
      transition_features [num_transitions, transition_dim]
                    │
                    ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                         MultiScalePetriRepresentation                            │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│  place_input: Linear(place_dim → place_hidden_dim)                              │
│  transition_input: Linear(transition_dim → transition_hidden_dim)               │
│                                                                                  │
│                    │                                                             │
│                    ▼                                                             │
│  ┌─────────────────────────────────────────────────────────────────────────┐    │
│  │              MultiScaleFusionLayer × num_layers                          │    │
│  │                                                                           │    │
│  │  每层执行:                                                                │    │
│  │                                                                           │    │
│  │  1. 计算全局上下文:                                                        │    │
│  │     context = cat(place_hidden.mean(), transition_hidden.mean())         │    │
│  │                                                                           │    │
│  │  2. 库所→变迁消息传递:                                                     │    │
│  │     t_msg_pre = einsum("tp,bph->bth", pre_t, p2t_pre(place_hidden))      │    │
│  │     t_msg_post = einsum("tp,bph->bth", post_t, p2t_post(place_hidden))   │    │
│  │     transition_update = transition_self + t_msg_pre + t_msg_post         │    │
│  │                       + transition_context(context)                       │    │
│  │                                                                           │    │
│  │  3. 变迁→库所消息传递:                                                     │    │
│  │     p_msg_pre = einsum("pt,bth->bph", pre, t2p_pre(transition_hidden))   │    │
│  │     p_msg_post = einsum("pt,bth->bph", post, t2p_post(transition_hidden))│    │
│  │     place_update = place_self + p_msg_pre + p_msg_post                   │    │
│  │                  + place_context(context)                                 │    │
│  │                                                                           │    │
│  │  4. 归一化 + FFN:                                                          │    │
│  │     transition_hidden = LayerNorm(transition_hidden + FFN(transition))   │    │
│  │     place_hidden = LayerNorm(place_hidden + FFN(place))                  │    │
│  └─────────────────────────────────────────────────────────────────────────┘    │
│                                                                                  │
│                    │                                                             │
│                    ▼                                                             │
│  graph_readout: Linear(place_hidden + transition_hidden → readout_dim)         │
│  transition_head: Linear(transition_hidden + readout → 1)                       │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
                    │
                    ▼
输出: PetriRepresentationOutput
      - place_embeddings [num_places, place_hidden_dim]
      - transition_embeddings [num_transitions, transition_hidden_dim]
      - graph_embedding [readout_dim]
      - transition_logits [num_transitions]


┌─────────────────────────────────────────────────────────────────────────────────┐
│                              完整数据链路                                         │
└─────────────────────────────────────────────────────────────────────────────────┘

1. 初始化阶段
   ─────────────────────────────────────────────────────────────────────────────
   配置参数 ──► PetriNet 实例 ──► Marking 初始状态
                              │
                              ├──► PetriNetGraph (图结构)
                              │
                              └──► PetriStateFeatureEncoder (特征编码器)
                                          │
                                          └──► 绑定 DeadlockController

2. 特征编码阶段
   ─────────────────────────────────────────────────────────────────────────────
   Marking ──► encode_places() ──► place_features [N×7~8]
            │
            └──► encode_transitions() ──► transition_features [M×12]
                        │
                        └──► deadlock_controller.analyze_state()
                                    │
                                    └──► controller_features

3. GCN 前向传播阶段
   ─────────────────────────────────────────────────────────────────────────────
   place_features ──┐
                    ├──► MultiScalePetriRepresentation ──► transition_logits
   transition_features ──┘                                    │
                                                              └──► graph_embedding

4. PPO 决策阶段
   ─────────────────────────────────────────────────────────────────────────────
   transition_logits ──► _mask_from_marking() ──► masked_logits
                                                │
                                                ▼
                                          _select_action()
                                                │
                                                ▼
                                           action (变迁索引)

5. 环境交互阶段
   ─────────────────────────────────────────────────────────────────────────────
   action ──► petri_net.enable(action) ──► 检查使能
            │
            └──► petri_net.launch(action) ──► next_marking
                        │
                        └──► _calc_reward() ──► reward

6. 训练更新阶段
   ─────────────────────────────────────────────────────────────────────────────
   episodes ──► _compute_gae() ──► advantages, returns
              │
              └──► _update_from_episodes() ──► PPO 损失计算
                                              │
                                              └──► optimizer.step()

7. 结果输出阶段
   ─────────────────────────────────────────────────────────────────────────────
   trans[] ──┐
             ├──► Result ──► 最终调度方案
   markings[] ──┘


                    ┌───────────────────────────────────┐
                    │   analyze_state(petri_net, marking) │
                    └─────────────────┬─────────────────┘
                                      │
                                      ▼
                    ┌───────────────────────────────────┐
                    │ 获取 enabled_actions (环境语义判定) │
                    └─────────────────┬─────────────────┘
                                      │
                    ┌─────────────────┴─────────────────┐
                    │                                   │
                    ▼                                   ▼
        ┌───────────────────┐              ┌───────────────────┐
        │ enabled_actions   │              │ enabled_actions   │
        │     为空?         │              │    不为空         │
        └─────────┬─────────┘              └─────────┬─────────┘
                  │                                  │
                  ▼                                  ▼
        ┌───────────────────┐          ┌─────────────────────────┐
        │ classify_deadlock │          │   第一层: 硬过滤         │
        │     _reason       │          │ 对每个action模拟一步     │
        └─────────┬─────────┘          └───────────┬─────────────┘
                  │                                │
                  ▼                    ┌───────────┴───────────┐
        ┌───────────────────┐          │                       │
        │ state_deadlock    │          ▼                       ▼
        │     = True        │  ┌───────────────┐    ┌───────────────┐
        └───────────────────┘  │ safe_actions  │    │hard_blocked   │
                               │   (通过)      │    │  _actions     │
                               └───────┬───────┘    └───────────────┘
                                       │
                          ┌────────────┴────────────┐
                          │                         │
                          ▼                         ▼
                ┌──────────────────┐      ┌──────────────────┐
                │ safe_actions     │      │ safe_actions     │
                │    为空?         │      │   不为空         │
                └────────┬─────────┘      └────────┬─────────┘
                         │                         │
                         ▼                         ▼
              ┌────────────────────┐    ┌─────────────────────────┐
              │ controller_empty   │    │ 检查是否触发第二层       │
              │ _fallback = True   │    │ _should_run_lookahead   │
              │ 回退到enabled       │    └───────────┬─────────────┘
              └────────────────────┘                │
                                          ┌────────┴────────┐
                                          │                 │
                                          ▼                 ▼
                                ┌──────────────┐   ┌──────────────┐
                                │ 不触发       │   │ 触发Lookahead│
                                │ controller   │   │ 有限深度搜索 │
                                │ =safe_actions│   └───────┬──────┘
                                └──────────────┘           │
                                                     ┌─────┴─────┐
                                                     │           │
                                                     ▼           ▼
                                              ┌──────────┐ ┌──────────┐
                                              │lookahead │ │soft_risk │
                                              │  _safe   │ │ _actions │
                                              └──────────┘ └──────────┘

```python
            "lambda_p": _env_int("GCN_PPO_HQ_LAMBDA_P", 256),
            "lambda_t": _env_int("GCN_PPO_HQ_LAMBDA_T", 64),
            "extra_p2t_rounds": _env_int("GCN_PPO_HQ_EXTRA_P2T_ROUNDS", 5),
            "gamma": _env_float("GCN_PPO_HQ_GAMMA", 0.999),
            "lr": _env_float("GCN_PPO_HQ_LR", 3e-4),
            "steps_per_epoch": _env_int("GCN_PPO_HQ_STEPS_PER_EPOCH", 6144),  # 12288
            "minibatch_size": _env_int("GCN_PPO_HQ_MINIBATCH_SIZE", 128),
            "ppo_epochs": _env_int("GCN_PPO_HQ_PPO_EPOCHS", 4),  # ppo更新轮数
            "target_kl": _env_float("GCN_PPO_HQ_TARGET_KL", 0.09),

            "entropy_coef_start": _env_float("GCN_PPO_HQ_ENTROPY_START", 0.15),##0.09
            "entropy_coef_end": _env_float("GCN_PPO_HQ_ENTROPY_END", 0.015),

            "temperature_start": _env_float("GCN_PPO_HQ_TEMPERATURE_START", 2.0),
            "temperature_end": _env_float("GCN_PPO_HQ_TEMPERATURE_END", 1.1),

            "reward_goal_bonus": _env_float("GCN_PPO_HQ_REWARD_GOAL", 1000.0),
            "reward_deadlock_penalty": _env_float("GCN_PPO_HQ_REWARD_DEADLOCK", 90.0), # 2000
            "reward_progress_weight": 2.0,
            "reward_repeat_penalty": _env_float("GCN_PPO_HQ_REWARD_REPEAT", 1.5),
            "reward_time_scale": _env_float("GCN_PPO_HQ_REWARD_TIME_SCALE", 1000.0),

            "beam_width": _env_int("GCN_PPO_HQ_BEAM_WIDTH", 100),
            "beam_depth": _env_int("GCN_PPO_HQ_BEAM_DEPTH", 800),

            "pool_eval_interval": _env_int("GCN_PPO_HQ_POOL_EVAL_INTERVAL", 4),  # 每4个epoch评估一次
            "curriculum_epochs": _env_int("GCN_PPO_HQ_CURRICULUM_EPOCHS", 4),  # 预热阶段epoch数
            
            "mask_cache_limit": _env_int("GCN_PPO_HQ_MASK_CACHE_LIMIT", 40000),
            "mixed_rollout": os.environ.get("GCN_PPO_HQ_MIXED_ROLLOUT", "1") == "1",
            "cross_env_gae": os.environ.get("GCN_PPO_HQ_CROSS_ENV_GAE", "1") == "1",
            "async_collection": os.environ.get("GCN_PPO_HQ_ASYNC_COLLECTION", "0") == "0",
            "envs_per_epoch": _env_int("GCN_PPO_HQ_ENVS_PER_EPOCH", 2),  # 每个epoch选2个环境进行训练
            "dynamic_curriculum": os.environ.get("GCN_PPO_HQ_DYNAMIC_CURRICULUM", "1") == "1",
            "curriculum_warmup_ratio": _env_float("GCN_PPO_HQ_CURRICULUM_WARMUP_RATIO", 0.3),
            "stochastic_num_rollouts": _env_int("GCN_PPO_HQ_STOCHASTIC_NUM_ROLLOUTS", 50),
            "stochastic_temperature": _env_float("GCN_PPO_HQ_STOCHASTIC_TEMPERATURE", 1.2),
            "use_deadlock_controller": os.environ.get("GCN_PPO_HQ_USE_DEADLOCK_CONTROLLER", "1") == "1",
```