diff_timelimit 数据集说明

1. 目标目录
   训练集目录为 `python_port/diff_timelimit`。
   目录中保留 45 个目标网文件：
   - 场景 1：`1-x-4`、`1-x-9`、`1-x-16`，其中 `x=1..5`
   - 场景 2：`2-x-4`、`2-x-9`、`2-x-16`，其中 `x=1..5`
   - 场景 3：`3-x-4`、`3-x-9`、`3-x-17`，其中 `x=1..5`

2. 片数修改规则
   文件名中的最后一个数字表示加工片数。
   对每个目标文件，按同场景同加工的已有模板生成，并同步修改：
   - 库所 `2` 的 token 数
   - `goalPlace` 中的 token 数

3. 模板来源
   当前 `diff_timelimit` 中的 45 个文件由 `resources` 中对应的 45 个网文件复制生成。

4. ptime 修改规则
   每个网文件的 `ptime` 条目均已处理，并满足：
   - 原始扰动规则保持不变：随机修改 `ceil(60%)` 的 `ptime`
   - 当前最终版本中，所有 `ptime` 数值都规整为 `5` 的倍数
   - 规整方式为按就近倍数取整；若结果小于 `5`，则取 `5`

5. ttime 修改规则
   `ttime` 仍按原始规则保留：
   - 随机修改 `ceil(60%)` 的条目
   - 新值位于原值 `50%` 范围内
   - 新值不等于原值

6. residenceTime 修改规则
   对含有 `residenceTime` 约束的网文件：
   - 随机修改 `ceil(60%)` 的条目
   - 新值位于原值 `50%` 范围内
   - 新值不等于原值
   - 当前最终版本中，`resources` 目录下所有网文件的 `residenceTime` 都已规整为 `100` 的倍数

7. 当前结果
   当前 `diff_timelimit` 中共 45 个网文件，且所有网文件的 `ptime` 都是 `5` 的倍数。
   当前 `resources` 目录下所有网文件的 `residenceTime` 都是 `100` 的倍数。
